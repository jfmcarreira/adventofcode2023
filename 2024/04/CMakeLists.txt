file(RELATIVE_PATH TARGET_NAME ${YEAR_FOLDER} ${CMAKE_CURRENT_SOURCE_DIR} )
file(GLOB_RECURSE SOURCES "${CMAKE_CURRENT_LIST_DIR}/${TARGET_NAME}.cpp")
set(EXECUTABLE_OUTPUT_PATH ${CMAKE_CURRENT_SOURCE_DIR})
add_executable(${TARGET_NAME} ${SOURCES})
