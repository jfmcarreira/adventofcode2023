{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "cmake",
			"label": "CMake: build",
			"command": "build",
			"targets": [
				"${command:cmake.buildTargetName}"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			},
		},
	]
}